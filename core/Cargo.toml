[package]
name = "readout_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "readout_core"
crate-type = ["staticlib", "rlib"]

[dependencies]
libc = "0.2"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
